<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.aplas.stuntingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StuntingApp">
        <activity android:name=".TracingActivity"></activity>
        <activity android:name=".DetailFoodHelpActivity" />
        <activity android:name=".FoodHelpActivity" />
        <activity android:name=".DetailNutritionActivity" />
        <activity android:name=".NutritionActivity" />
        <activity android:name=".DetailInfoStuntingActivity" />
        <activity android:name=".InfoStuntingActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>